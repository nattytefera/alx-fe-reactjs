// Use the arrow function syntax for consistency.
const MainContent = () => {
    return (
        <main>
            <p>I love to visit New York, Paris, and Tokyo.</p>
        </main>
    );
};

export default MainContent;
